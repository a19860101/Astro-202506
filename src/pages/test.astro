---
const title = 'My Blog'
const posts = ['post1','post2','post3']
const user = {
	name: 'John',
	mail: 'asdf@gmail.com'
} 
const products = [
    {
        name: 'iPhone 16 Pro',
        price: 36900,
        storage: '128GB' 
    },
    {
        name: 'iPhone 16 Pro',
        price: 40400,
        storage: '256GB' 
    },
    {
        name: 'iPhone 16 Pro',
        price: 47400,
        storage: '512GB' 
    },
]
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <div>{title}</div>
    <div>
        {
            // products.map(function(data){return <div>{data.name}/{data.storage}:${data.price}</div>})
            // products.map((data)=>{return <div>{data.name}/{data.storage}:${data.price}</div>})
            products.map((data) => <div>{data.name}/{data.storage}:${data.price}</div> )
        }
    </div>
    <div>
        {
            // posts.map(function(data){return <ul><li>{data}</li></ul>})
            posts.map(data => <ul><li>{data}</li></ul>)
        }
    </div>
    <script>

        // const datas = ['apple','apple','egg','egg','egg','banana',['apple','egg',['egg','banana']]].flat(2);
        // const uniqueData = new Set(datas)
        // let uniqueDataArray = Array.from(uniqueData)
        // let uniqueDataArray = [...uniqueData]

        // console.log(uniqueDataArray)

        // const u = uniqueDataArray.filter(post => post == 'egg')
        // const u = uniqueDataArray.includes('asdf')
        // console.log(u)


        const posts = [
            {
                title: '001',
                tags: ['apple','banana']
            },
            {
                title: '002',
                tags: ['banana','cat']
            },
            {
                title: '003',
                tags: ['banana', 'dog']
            }
        ]
        const uniqueTags = [...new Set(posts.map(post => post.tags).flat())]
        console.log(uniqueTags)

        const filterPosts = posts.filter( post => post.tags.includes('dog'))

        console.log(filterPosts)

    </script>
</body>
</html>